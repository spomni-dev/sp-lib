Common:
  y Create file structure @done (2017-12-18 17:01)
  o Create test environment
  o Develop main namespace
  o Add namespace "bem"
  o Add namespace "assert"
  o Add namespace "common"
  o Add method "exposeChildsToWindow()"
  o Add task numbering system

@namespace bem:
  o Comment namespace "bem"

@namespace assert:
  o Comment namespace "assert"
  
@namespace common
  o Comment namespace "common"
  o Add function "includeScript()"
  o Add function "includeStylesheet()"